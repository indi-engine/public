/**
 * Base class for all controller actions instances, that operate with some certain rows
 */
Ext.override(Indi.lib.controller.action.Row, {
    southItemA: function() {
        return [];
    }
});
